{
  "name": "serverless-users-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "serverless package",
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "remove:dev": "serverless remove --stage dev --verbose",
    "typecheck": "tsc --noEmit",
    "offline": "serverless offline --stage local --httpPort 4000",
    "offline:local": "npm run dynamodb:up && npm run dynamodb:wait && npm run dynamodb:init && npm run offline"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.981.0",
    "@aws-sdk/client-sqs": "^3.981.0",
    "@aws-sdk/lib-dynamodb": "^3.981.0",
    "aws-lambda": "^1.0.7",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.3",
    "express": "^5.2.1",
    "typeorm": "^0.3.28",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.0",
    "serverless": "^3.40.0",
    "serverless-offline": "^13.9.0",
    "serverless-plugin-typescript": "^2.1.5",
    "typescript": "^5.9.3"
  }
}