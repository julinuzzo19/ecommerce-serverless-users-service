{
  "name": "serverless-users-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dynamodb:up": "docker compose up -d dynamodb",
    "dynamodb:down": "docker compose down",
    "dynamodb:wait": "node scripts/dynamodb/wait.js",
    "dynamodb:init": "node scripts/dynamodb/init-users-table.js",
    "package": "serverless package",
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "remove:dev": "serverless remove --stage dev",
    "typecheck": "tsc --noEmit",
    "offline": "serverless offline --httpPort 4000",
    "offline:local": "npm run dynamodb:up && npm run dynamodb:wait && npm run dynamodb:init && npm run offline"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.958.0",
    "@aws-sdk/lib-dynamodb": "^3.958.0",
    "aws-lambda": "^1.0.7",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.3",
    "express": "^5.2.1",
    "typeorm": "^0.3.28",
    "uuid": "^13.0.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "@types/uuid": "^10.0.0",
    "serverless": "^3.38.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^14.4.0",
    "serverless-plugin-typescript": "^2.1.5",
    "typescript": "^5.9.3"
  }
}